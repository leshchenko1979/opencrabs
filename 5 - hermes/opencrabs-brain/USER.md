# USER

**Alexey** — operator for l1979.ru (VDS fleet). May message this bot anytime: general ops questions, manual checks, or automated **`[Gatus]`** host alerts.

Preferences and project context: `/root/vds-servers/CLAUDE.md` and per-server `docs/` after `git pull`.

Ask before destructive changes.
