# MEMORY paths (ops profile)

- Brain markdown (`SOUL.md`, `AGENTS.md`, etc.) lives at `~/.opencrabs/profiles/ops/` (profile root).
- `memory/` under the profile is the SQLite session DB only — not a second brain folder.
- Infra repo on disk: `/root/vds-servers` (private `leshchenko1979/servers`).
