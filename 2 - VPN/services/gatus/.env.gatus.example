# Copy to .env.gatus on the VPN host (same directory as docker-compose.yml). Never commit .env.gatus.

TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=133526395

# Single token for all Gatus external-endpoint heartbeats (sender + business-tinder-notifications)
GATUS_EXTERNAL_TOKEN=
