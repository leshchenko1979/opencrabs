- **2026-05-04**: Disk cleanup — reduced `/` from 88% to 76% (1.2GB → 2.4GB free). Wiped `/var/log/btmp*` (63MB) and rotated auth/syslog files; rebuilt swap from 1.5GB to 512MB. Services survived cleanly (n8n, caddy active, n8n healthz 200). Unified diagnostics updated with systemd service checks for box 4 (no Docker). Created missing `scripts/diagnostics.sh` for box 4. Root README + CLAUDE.md + services.md updated with current disk/service state.

- **2026-04-08**: **ZeroClaw native binary migration**: Docker removed (apt-get remove docker-ce docker-ce-cli docker-ce-rootless-extras docker-compose-plugin docker-buildx-plugin); zeroclaw data migrated from Docker volume (`/var/lib/docker/volumes/zeroclaw-data/_data`) to host directory `/root/zeroclaw-data/`; ZeroClaw runs as user-level systemd service (native Rust binary at `/root/.cargo/bin/zeroclaw daemon`); port changed from Docker 18789→42617 to native 42617; Caddy updated: `claw.l1979.ru` → `127.0.0.1:42617`; docs updated: `services.md` (ZeroClaw runtime), `maintenance.md` (service commands), `README.md` (stack description). Docker artifacts left on disk (`/opt/zeroclaw`, `/var/lib/docker/volumes`) for manual cleanup. Gatus config unchanged (health check URL unchanged).

# Progress log

## 2026-03-30

- **n8n upgraded to 2.13.4**: freed disk space (journalctl vacuum, apt autoremove, rm npm cache) then `npm install -g n8n@latest`; disk filled to 100% during install multiple times; eventually succeeded; n8n healthz 200.
- **n8n execution history**: Added `EXECUTIONS_DATA_MAX_AGE=48` to `/etc/n8n.env` (2-day retention); restarted n8n service.
- **OpenClaw on remote VDS**: installed via `curl -fsSL https://openclaw.ai/install.sh | bash` (v2026.3.28); Telegram bot pairing approved for user `133526395`; user-level systemd unit `openclaw-gateway` enabled on port `18789`. Docs updated: `docs/services.md` (new OpenClaw section), `docs/maintenance.md` (services section updated).
- **deploy-openclaw.sh discrepancy**: script uses `npm install -g openclaw@latest` + system-level systemd unit; actual install used `install.sh` + user-level systemd unit.

## 2026-03-29

- **OpenClaw installed**: `curl -fsSL https://openclaw.ai/install.sh | bash` — installed v2026.3.28; Telegram bot configured with pairing approved for user 133526395; Gateway runs on `127.0.0.1:18789`; systemd user service enabled.
- **Disk cleanup**: freed ~1.5GB (rm old kernel headers, apt/apt lists, npm cache, openclaw install attempt debris); reduced swap 2GB → 1.5GB.
- **diagnostics.sh updated**: added OpenClaw checks (CLI version, systemd user service, healthz port 18789, port filter updated to include 18789).
- **servers/README.md updated**: operations_4 now lists OpenClaw AI agent.

## 2026-03-23

- **VDS `2.27.120.75`**: refreshed NodeSource `setup_22.x` + `apt install nodejs` — already **v22.22.1** (no newer package). Cleaned `/root/.npm` cache, cleared `/var/lib/n8n/.cache/*`, `apt-get clean` / `autoremove`, `journalctl --vacuum-size=80M` (freed ~0). **`/` 87% → ~71%**; restarted `n8n`, healthz 200. `apt upgrade`: `libtiff6`, `python3-openssl`.
- **Unresponsive host investigate** (`2.27.120.75`): unclean shutdown — `system.journal corrupted or uncleanly shut down`; journal silent ~16:42–17:37 MSK then reboot 17:37; prior boot had no OOM / no-space / I/O-error lines; ~1Gi RAM, `/` 87%; kernel clocksource “long readout” Mar 22; n8n “Connection timeout” morning Mar 23. Conclusion: freeze/hard reset — root cause not logged. Post-reboot: n8n/caddy active, healthz 200.
- `.vscode/tasks.json`: all tasks `group.kind` `build`, `isDefault` false (Run Build Task lists them; no single default).
- `.vscode/tasks.json`: SSH task `SSH: VDS` → `root@2.27.120.75`.

## 2026-03-21

- **Mac backup**: `../mac-workstation-backup/README.md` — ops4 included in `backup-ops3-ops4.sh`; `docs/maintenance.md` updated.
- Repo cleanup: removed duplicate `.cursor/rules/documentation-structure.mdc` (covered by `memory-bank.mdc`); fixed `.vscode/tasks.json` cwd → `${workspaceFolder}`; deleted `logs/diagnostics.log` (regenerated by `diagnostics.sh`); trimmed `progress-log` 2026-03-20; `docs/maintenance.md` backup link removed; `services/n8n/docker-compose.yml` header comment.

- Production-only hostnames: docs, `.env*`, scripts, reference compose (no staging/CRM Traefik).
- **VDS `2.27.120.75`**: `/etc/caddy/Caddyfile` now only `n8n.l1979.ru`; `/etc/n8n.env` `N8N_HOST` / editor / webhook → `n8n.l1979.ru`; `systemctl reload caddy` + `restart n8n`; `healthz` 200.
- **Code node `js-yaml`**: `/etc/n8n.env` lacked `NODE_FUNCTION_ALLOW_*` (Docker reference had it; systemd template did not). Added `NODE_FUNCTION_ALLOW_BUILTIN=*` + `NODE_FUNCTION_ALLOW_EXTERNAL=js-yaml`; `bootstrap-server.sh` + `docs/services.md` updated; `restart n8n`.

## 2026-03-20

- Migration + scaffold: DNS → `2.27.120.75`; legacy n8n/Traefik removed; data migrated from `94.250.254.232`; bootstrap/diagnostics/migration scripts; reference compose + docs. Disk ~92% noted.
